// var express = require("express");
// var routerNote = express.Router();
// var noteController = require("../../controllers/note");

// routerNote.get("/:id", noteController.findOne);
// routerNote.post("/", noteController.create);
// routerNote.delete("/:id", noteController.delete);

// module.exports = routerNote;